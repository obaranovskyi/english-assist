import { MatSnackBarConfig } from '@angular/material/snack-bar';

export const NOTIFICATION_CONFIG: MatSnackBarConfig<any> = {
  horizontalPosition: 'start',
  verticalPosition: 'bottom',
  duration: 3000,
};
